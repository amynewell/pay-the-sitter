require_relative "../helper"
require_relative "../../lib/pay_the_sitter/calendar_api"


describe PayTheSitter::CalenderAPI do
  # let(:page) do
  #   posts_dir = "#{File.dirname(__FILE__)}/../fixtures/sample_app/_posts"
  #   Jackal::Page.new("#{posts_dir}/2012-05-07-first-post.markdown")
  # end

  it "must retrieve events from the specified calendar" do
    
  end

  it "must retrieve the events within the specified date range" do
   
  end

  it "must convert the dates to and from the Google API format correctly" do
   
  end

  it "must add up the number of hours of the events correctly" do
   
  end

  it "must fail nicely if the google calendar api is not available" do
   
  end

  it "must also fail nicely if it can't find the specified calendar, with a useful message" do
   
  end

  it "ideally should not puke when we stay out past midnight. lulz" do
   
  end

end